# LEXSY_ASSIGNMENT

Steps to follow to run the applicaiton:
1. Download the project file
2. use the following commands to run it on your local host of the application

   #cd lexsy_legal_assistant_unified_v2(or file path on your pc)
   #python -m venv .venv
   #pip install -r requirements.txt
   #python app.py
